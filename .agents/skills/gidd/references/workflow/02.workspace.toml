[zh-CN."*.current-worktree.*"]
title = "工作目录准备"
body = '''
在任务开始时的工作目录及其当前本地分支上工作，不另建专用 worktree 或开发分支；将该分支记录为目标分支和工作分支，保存来源目录及起始提交，继续任务时沿用记录。
留意已有改动、并行任务及当前目录的 GIDD 配置是否适用。
说明实际工作目录和比较基准，便于用户查看改动。
'''

[zh-CN."*.dedicated-worktree.*"]
title = "工作目录准备"
body = '''
将任务开始时来源工作目录的当前本地分支记录为目标分支，保存来源目录和起始提交，并从该分支派生本任务的开发分支及专用 worktree；继续任务时沿用记录和仍需使用的工作区。
采用 PR 合并时，先确认配置工作远端已有同名目标分支，不自动改用远端默认分支。
留意已有改动、并行任务及当前目录的 GIDD 配置是否适用。
提供专用 worktree 的路径和比较基准，便于用户查看改动。
'''

[en."*.current-worktree.*"]
title = ""
body = ""

[en."*.dedicated-worktree.*"]
title = ""
body = ""
